{
  "name": "payhermes",
  "private": true,
  "engines": {
    "node": ">=20",
    "pnpm": ">=10"
  },
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "type-check": "pnpm --filter web type-check"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
